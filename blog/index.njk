---
layout: blog-layout.njk
title: Blog
description: Free guides, tips and strategies to grow your social media presence in India — no fluff, just results.
pagination:
  data: collections.blog
  size: 6
  alias: posts
permalink: "/blog/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}index.html"
---

<section class="blog-hero">
  <h1>The BharatLikes <span>Blog</span></h1>
  <p>Free guides, tips and strategies to grow your social media presence in India — no fluff, just results.</p>
</section>

<section class="blog-section">
  <div class="blog-grid">
    {% for post in posts %}
    <article class="blog-card">
      <div class="blog-card-thumb">
        {{ post.data.emoji or "📈" }}
      </div>
      <div class="blog-card-body">
        <div class="blog-card-meta">
          <span class="blog-tag">
            <a href="/blog/category/{{ post.data.category | slug }}/">{{ post.data.category }}</a>
          </span>
          <span class="blog-date">{{ post.date | postDate }}</span>
        </div>
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        <p>{{ post.data.description }}</p>
        <a href="{{ post.url }}" class="blog-read-more">Read Article <i class="fa-solid fa-arrow-right"></i></a>
      </div>
    </article>
    {% endfor %}
  </div>
</section>

{% if pagination.pages.length > 1 %}
<div class="pagination">
  {% if pagination.href.previous %}
    <a href="{{ pagination.href.previous }}">← Previous</a>
  {% endif %}
  {% if pagination.href.next %}
    <a href="{{ pagination.href.next }}">Next →</a>
  {% endif %}
</div>
{% endif %}

<section class="blog-cta">
  <h2>Ready to Put These Tips Into Action?</h2>
  <p>Let BharatLikes accelerate your growth — real followers, real engagement, delivered fast.</p>
  <a href="/products/pricing/" class="btn-primary">View Pricing & Packages</a>
</section>
